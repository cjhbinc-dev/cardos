# Local Netlify folder
.netlify

# mTLS certificates / key material — never commit these (dir removed with Teller, rule stays)
/certs
*.pem
*.key

# Local environment variables
.env
.env.*

# Dependencies
node_modules/

# Stray backup files (kept locally, not tracked)
index (13).html
CardOS (1).html
big-dreamers-materials.html

# Claude session files
.claude/
